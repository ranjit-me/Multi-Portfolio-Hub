# Example Environment Variables for S3 Configuration
# Copy this file to .env or set these variables in your deployment environment

# AWS S3 Configuration
AWS_S3_ENABLED=true
AWS_S3_BUCKET=your-portfolio-bucket-name
AWS_REGION=us-east-1
AWS_ACCESS_KEY=your-aws-access-key
AWS_SECRET_KEY=your-aws-secret-key

# Local Upload Configuration (when S3 is disabled)
UPLOAD_DIR=uploads/photos

# MongoDB Configuration (if needed to override)
# MONGODB_URI=mongodb://localhost:27017
# MONGODB_DATABASE=portfolio

# JWT Configuration (if needed to override)
# JWT_SECRET=your-super-secure-jwt-secret-key
# JWT_EXPIRATION=86400000

# Server Configuration
# SERVER_PORT=8081

# CORS Configuration
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
